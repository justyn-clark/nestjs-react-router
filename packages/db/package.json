{
  "name": "@nestjs-react-router/db",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "npx biome lint .",
    "lint:fix": "npx biome lint --write .",
    "format": "npx biome format --write .",
    "drizzle:push": "pnpm --filter @nestjs-react-router/env build && node scripts/ensure-database.mjs && drizzle-kit push",
    "drizzle:generate": "pnpm --filter @nestjs-react-router/env build && drizzle-kit generate:pg",
    "drizzle:studio": "pnpm --filter @nestjs-react-router/env build && drizzle-kit studio"
  },
  "dependencies": {
    "@nestjs-react-router/env": "workspace:*",
    "dotenv": "^17.4.0",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.12.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.2"
  }
}
